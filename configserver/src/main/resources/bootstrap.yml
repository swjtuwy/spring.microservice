spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${config.repo.uri:https://github.com/swjtuwy/spring-cloud-example}
          searchPaths: config-repo

server:
  port: 8888

eureka.client.serviceUrl.defaultZone: http://localhost:8761/eureka/
